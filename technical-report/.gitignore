# LaTeX build artefacts, including the built report itself. Rebuild it with `make`.
# Figures under figures/ are inputs, not artefacts, and stay tracked.
/report.pdf
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
